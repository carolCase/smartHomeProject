spring.application.name=settings_project_fullstack

# Database connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/settings_project_fullstack
spring.datasource.username=postgres
spring.datasource.password=Castroli82
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Connection pool settings (optional, if using HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2